sylius_grid:
    grids:
        sylius_admin_order:
            actions:
                main:
                    export:
                        type: links
                        label: "Export"
                        options:
                            icon: "download"
                            links:
                                excel:
                                    label: "Excel"
                                    icon: "file excel outline"
                                    route: vcare_admin_export_orders
                                    parameters:
                                        criteria: $criteria
