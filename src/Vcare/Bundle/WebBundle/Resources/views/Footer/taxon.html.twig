{% if taxons|length %}
    <ul>
        {% for taxon in taxons %}
            <li><a href="{{ path('route_shop_taxon_show', {code: taxon.code}) }}">{{ taxon.name }}</a></li>
        {% endfor %}
    </ul>
{% endif %}
