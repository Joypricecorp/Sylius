{% set id = app.request.attributes.get('id') %}
{{ form_start(form, {'action': path('sylius_shop_account_address_book_set_as_default', {'id': id}), 'method': 'PATCH', 'attr': {style: 'display:inline'}}) }}
    <input type="hidden" name="{{ form.defaultAddress.vars['full_name'] }}" value="{{ id }}" />
    <button type="submit" class="btn btn-link btn-sm">
        <i class="fa fa-bookmark-o"></i> {{ "Default_address"|trans }}
    </button>
    {{ form_row(form._token) }}
{{ form_end(form, {'render_rest': false}) }}