{% from 'VcareWebBundle:Product:macros.html.twig' import list %}

<div class="product-list-homepage">
    <div class="row">
        {{ list(products) }}
    </div>

    <div class="text-align-center" style="margin-bottom: 1rem">
        <a href="{{ path('sylius_shop_product_index') }}" class="btn btn-lg btn-primary">{{ "vcare.ui.more_products"|trans }}</a>
    </div>
</div>
